<div class="row d-flex justify-content-around">
  <%= render 'admin/shared/side_bar' %>
  <div class="col-8 d-flex flex-column">
    <h1>Nouveau Test</h1>
    <%= simple_form_for @test, url: admin_tests_path do |f| %>
      <%= f.input :category_id, collection: Category.all %>
      <%= f.input :name %>
      <%= f.input :duration %>
      <%= f.input :description %>
      <%= f.input :solution %>
      <%= f.submit "Créer", class: "buttons button-dark" %>
    <% end %>
  </div>
</div>
