<div class="row d-flex justify-content-around">
  <%= render 'admin/shared/side_bar' %>
  <div class="col-8 d-flex flex-column">
    <h1>Game</h1>
    <%= simple_form_for @game, url: admin_game_path(@game) do |f| %>
      <p>ID: <%= @game.id %></p>
      <%= f.input :name %>
      <%= f.input :category_id, collection: Category.all %>
      <%= f.input :duration %>
      <%= f.input :description %>
      <%= f.input :solution %>
      <%= f.submit "Mettre à jour", class: "buttons button-dark" %>
    <% end %>
  </div>
</div>
