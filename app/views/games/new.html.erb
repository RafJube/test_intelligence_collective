<div class="container">
  <div class="row justify-content-between">
    <div class="d-flex justify-content-center">
      <h1><%= @test.name.upcase %> <i class="fa-solid fa-circle-info" data-bs-toggle="modal" data-bs-target="#testDescription"></i></h1>
    </div>
      <div class="d-flex justify-content-center">
        <div class="col-8">
          <%= simple_form_for [@test, @game], url: test_games_path(@test.name) do |f| %>
              <h3>Selectionnez le nombre de joueur pour cette partie:</h3>
              <%= f.input :players_number,
                required: true,
                label: false,
                collection: [1, 2, 3, 4],
                :input_html => { class: 'text-field' } %>
              <%= f.submit "Créer la partie", class: "buttons button-dark" %>
          <% end %>
        </div>
      </div>
  </div>
  <%= render 'shared/modal_test_description' %>
</div>
